<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.springbootdemo.persistence.MBLogDAO">
    <select id="insertDiary" parameterType="Dairy">
        INSERT INTO DIARY(USERNAME,DATE,OPERATION) VALUES(#{username}, #{date}, #{operation})
    </select>
</mapper>