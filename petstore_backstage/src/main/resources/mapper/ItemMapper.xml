<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.persistence.ItemMapper">
    <select id="getItem" parameterType="string" resultType="Item">
    select
      I.ITEMID,
      LISTPRICE,
      UNITCOST,
      SUPPLIER AS supplierId,
      I.PRODUCTID AS "product.productId",
      NAME AS "product.name",
      DESCN AS "product.description",
      CATEGORY AS "product.categoryId",
      STATUS,
      ATTR1 AS attribute1,
      ATTR2 AS attribute2,
      ATTR3 AS attribute3,
      ATTR4 AS attribute4,
      ATTR5 AS attribute5,
      QTY AS quantity
    from ITEM I, INVENTORY V, PRODUCT P
    where P.PRODUCTID = I.PRODUCTID
      and I.ITEMID = V.ITEMID
      and I.ITEMID = #{value}
  </select>

    <select id="getInventoryQuantity" parameterType="string" resultType="int">
    SELECT QTY AS value
    FROM INVENTORY
    WHERE ITEMID = #{itemId}
  </select>

  <select id="getItemListByProduct" parameterType="string" resultType="Item">
    SELECT
      I.ITEMID,
      LISTPRICE,
      UNITCOST,
      SUPPLIER AS supplierId,
      I.PRODUCTID AS "product.productId",
      NAME AS "product.name",
      DESCN AS "product.description",
      CATEGORY AS "product.categoryId",
      STATUS,
      ATTR1 AS attribute1,
      ATTR2 AS attribute2,
      ATTR3 AS attribute3,
      ATTR4 AS attribute4,
      ATTR5 AS attribute5
    FROM ITEM I, PRODUCT P
    WHERE P.PRODUCTID = I.PRODUCTID
    AND I.PRODUCTID = #{value}
  </select>

  <select id="updateItem">
    update ITEM set listprice=#{listPrice}, attr1=#{attribute1} where itemid=#{itemId}
  </select>

  <select id="updateQuantity">
    update inventory set qty=#{quantity} where itemid=#{itemId}
  </select>

  <select id="deleteItem">
    delete from ITEM I,inventory V where I.itemid=V.itemId AND I.itemid=#{itemId}
  </select>

  <select id="insertItem" parameterType="item">
    insert into ITEM values (#{itemId}, #{productId}, #{listPrice}, #{unitCost}, #{supplierId}, #{status},
    #{attribute1}, #{attribute2}, #{attribute3}, #{attribute4}, #{attribute5});
  </select>
  <select id="insertItemQuantity">
    insert into inventory values(#{itemId}, #{quantity})
  </select>

</mapper>