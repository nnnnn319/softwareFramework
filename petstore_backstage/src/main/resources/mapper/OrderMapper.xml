<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.persistence.OrderMapper">
    <select id="getOrder" resultType="Order" parameterType="int">
    select
      BILLADDR1 AS billAddress1,
      BILLADDR2 AS billAddress2,
      BILLCITY,
      BILLCOUNTRY,
      BILLSTATE,
      BILLTOFIRSTNAME,
      BILLTOLASTNAME,
      BILLZIP,
      SHIPADDR1 AS shipAddress1,
      SHIPADDR2 AS shipAddress2,
      SHIPCITY,
      SHIPCOUNTRY,
      SHIPSTATE,
      SHIPTOFIRSTNAME,
      SHIPTOLASTNAME,
      SHIPZIP,
      CARDTYPE,
      COURIER,
      CREDITCARD,
      EXPRDATE AS expiryDate,
      LOCALE,
      ORDERDATE,
      ORDERS.ORDERID,
      TOTALPRICE,
      USERID AS username,
      STATUS
    FROM ORDERS, ORDERSTATUS
    WHERE ORDERS.ORDERID = #{value}
      AND ORDERS.ORDERID = ORDERSTATUS.ORDERID
  </select>

<select id="getOrdersByUsername" parameterType="string" resultType="Order">
    SELECT
    BILLADDR1 AS billAddress1,
    BILLADDR2 AS billAddress2,
    BILLCITY,
    BILLCOUNTRY,
    BILLSTATE,
    BILLTOFIRSTNAME,
    BILLTOLASTNAME,
    BILLZIP,
    SHIPADDR1 AS shipAddress1,
    SHIPADDR2 AS shipAddress2,
    SHIPCITY,
    SHIPCOUNTRY,
    SHIPSTATE,
    SHIPTOFIRSTNAME,
    SHIPTOLASTNAME,
    SHIPZIP,
    CARDTYPE,
    COURIER,
    CREDITCARD,
    EXPRDATE AS expiryDate,
    LOCALE,
    ORDERDATE,
    ORDERS.ORDERID,
    TOTALPRICE,
    USERID AS username,
    STATUS
    FROM ORDERS, ORDERSTATUS
    WHERE ORDERS.USERID = #{value}
    AND ORDERS.ORDERID = ORDERSTATUS.ORDERID
    ORDER BY ORDERDATE
</select>
<select id="updateOrderStatus">
    update ORDERSTATUS set status=#{status} where orderid=#{orderId}
</select>
    <select id="deleteOrder">
        delete from ORDERS where orderid=#{orderId}
    </select>
    <select id="deleteOrderStatus">
        delete from ORDERSTATUS where orderid=#{orderId}
    </select>
    <select id="updateOrder" parameterType="Order">
        update ORDERS set billaddr1=#{billAddress1}, billaddr2=#{billAddress2},
         billcity=#{billCity}, billstate=#{billState}, billzip=#{billZip}, billcountry=#{billCountry},
         shipaddr1=#{shipAddress1}, shipaddr2=#{shipAddress2}, shipcity=#{shipCity},
        shipstate=#{shipState}, shipzip=#{shipZip}, shipcountry=#{shipCountry} where
        orderid=#{orderId}
    </select>
</mapper>